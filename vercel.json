{
  "buildCommand": "pnpm --filter @workspace/netkit run build",
  "outputDirectory": "artifacts/netkit/dist/public",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}